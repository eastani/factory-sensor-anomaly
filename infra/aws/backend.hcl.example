# Copy to backend.hcl and fill in real values.
# Init with: terraform init -backend-config=backend.hcl

bucket         = "factory-sensor-anomaly-tfstate-<YOUR-ACCOUNT-ID>"
dynamodb_table = "factory-sensor-anomaly-tfstate-locks"
